{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/src/**/*.{js,jsx}": [
      "eslint --max-warnings=0",
      "prettier --check"
    ],
    "backend/**/*.js": [
      "eslint --max-warnings=0",
      "prettier --check"
    ]
  }
}
